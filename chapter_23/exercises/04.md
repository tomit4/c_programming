Q: Using `isalpha` and `isalnum`, write a function that checks whther a string
has the syntax of a C identifier (it consists of letters, digits, and
underscores, with a letter or underscore at the beginning).

A:
