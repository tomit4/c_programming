/* Consider the following program: */

#include <stdio.h>

int main(void)
{
	printf("Parkinson's Law:\nWork expands so as to ");
	printf("fill the time\n");
	printf("available for its completion.\n");
	return 0;
}

/* Q(a): Identify the directives and statements in this program */
/* Q(b): What output does the program produce? */

/* A(a): The directives are the `#include <stdio.h>` and the statements are the
 * printf and return statements*/

/* A(b): The output is as follows:
 * Parkinson's Law:
 * Work expands so as to fill the time
 * available for its completion.
 * */
